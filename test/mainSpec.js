describe('sum method', function(){
  it('should return the sum of the two parameters', function(){
    expect(sum(1, 2)).toBe(1 + 2);
  });
});

describe('divide method', function(){
  it('should return the division of the two parameters', function(){
    expect(divide(2, 1)).toBe(2 / 1);
  });
});
